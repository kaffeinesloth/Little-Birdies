# Optional Supabase Realtime for the customer store chat.
# Leave blank for the final fallback demo; the widget will use backend polling.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend URL used by a non-Docker Vite build.
VITE_API_BASE_URL=http://localhost:8000
